CREATE TABLE ods_loan_trx
(
   date date,
   customerId varchar(20),
   paymentPeriod int,
   loanAmount	double, 
   currencyType varchar(3),
   evaluationChannel varchar(20),
   interest_rate double,
);